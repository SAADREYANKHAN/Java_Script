let marks = 90;
switch (marks) {
    case (marks >= 90):
        console.log("Grade A");

    case (marks >= 80 && marks < 90):
        console.log("Grade B");

    case (marks <= 50):
        console.log("Fail");
}